services:
  - type: web
    name: fastapi-backend
    runtime: python
    buildCommand: ""
    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000
    envVars:
      - key: MONGO_URI
        value: ${MONGO_URI}
      - key: MONGO_DB_NAME
        value: ${MONGO_DB_NAME}
